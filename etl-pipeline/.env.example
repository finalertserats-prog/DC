# Environment Variables (copy to .env and fill in)
VITE_API_URL=http://localhost:4000
PORT=4000

# Server Passwords (move to env in production)
AIRFLOW_PASSWORD=techSophy
STAGING_PASSWORD=tsLoader
